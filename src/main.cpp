#include "ExchangeSystem.h"
#include <iostream>
using namespace std;

int main() {
    ExchangeSystem system;  // initialize system (loads data)
    system.run();           // start menu loop
    return 0;
}
